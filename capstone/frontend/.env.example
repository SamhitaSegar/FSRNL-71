# Backend API base URL. In dev the Vite proxy forwards /api to the backend,
# so this can be left unset (falls back to "/api").
VITE_API_URL=/api

# Razorpay PUBLIC key id (rzp_test_... or rzp_live_...).
# This is safe to expose in the frontend. NEVER put the key secret here.
VITE_RAZORPAY_KEY_ID=
